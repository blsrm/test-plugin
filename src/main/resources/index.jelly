<?jelly escape-by-default='true'?>
<div>
  This plugin reads the build workspace to generate VCS (git or svn) rich html diffs from current and previous revisions and trigger Jobs based on the controller files changed and commit message
</div>
